const MoviesListTitle = ({ title }) => {
  return (
    <h1 className="font-semibold text-xl md:text-2xl mb-2 text-primary dark:text-white">
      {title}
    </h1>
  );
};

export default MoviesListTitle;
